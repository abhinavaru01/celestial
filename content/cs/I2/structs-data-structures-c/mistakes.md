# Common Mistakes — Structs & Building Data Structures in C

> Every learner hits these — spot them before they cost you.

- Forgetting to allocate memory for struct nodes.

- Losing the head pointer of a linked list.

- Not freeing nodes, causing leaks.

