# Common Mistakes — Input, Output & Simple Interaction

> Every learner hits these — spot them before they cost you.

- Forgetting input() returns a string (convert for arithmetic).

- Concatenating numbers and strings with +.

- Unclear or unlabelled output.

