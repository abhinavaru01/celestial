# Common Mistakes — Booleans & Conditionals

> Every learner hits these — spot them before they cost you.

- Using = (assignment) instead of == (comparison) in a condition.

- Wrong indentation changing which code is inside the if.

- Misordering elif branches so one is unreachable.

